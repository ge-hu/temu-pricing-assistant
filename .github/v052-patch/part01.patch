 async function performExport(){if(!state.books.length)return;const storeAssignments=await resolveExportHistoryStoreAssignments();if(storeAssignments===null){$('exportMsg').textContent='已取消导出：尚未确认多店铺SPU的历史归属。';$('exportMsg').className='hint warn';return}const ts=timestamp(),isSingle=state.books.length===1,suggestedName=isSingle?outputExcelName(state.books[0],ts):`多店铺_${ts}.zip`;$('exportMsg').textContent='正在生成…';$('exportMsg').className='hint';let td=0,tu=0,ti=0,tiu=0,tn=0,saveMode='download-unverified';const desktopMode=!!window.temuDesktop?.workspaceSaveProcessed;let sourceRenamed=0,sourceRenameWarnings=[];if(desktopMode){for(const b of state.books){const r=await buildProcessedBook(b);td+=r.deleted;tu+=r.updated;ti+=r.invDeleted;tiu+=r.invUpdated;tn+=r.unknown;const name=outputExcelName(b,ts),saved=await desktopSaveProcessedBook(b,r,name);if(saved?.sourceRenamedTo)sourceRenamed++;if(saved?.renameWarning)sourceRenameWarnings.push(`${b.file?.name||name}：${saved.renameWarning}`)}saveMode='desktop-workspace'}else if(isSingle){const b=state.books[0],r=await buildProcessedBook(b);td+=r.deleted;tu+=r.updated;ti+=r.invDeleted;tiu+=r.invUpdated;tn+=r.unknown;saveMode=await saveBlobPreferred(r.blob,suggestedName)}else{const pack=new JSZip(),used=new Set();for(const b of state.books){const r=await buildProcessedBook(b);td+=r.deleted;tu+=r.updated;ti+=r.invDeleted;tiu+=r.invUpdated;tn+=r.unknown;let name=outputExcelName(b,ts),n=2;while(used.has(name)){name=outputExcelName({...b,storeName:`${b.storeName}_${n++}`},ts)}used.add(name);pack.file(name,r.blob)}const blob=await pack.generateAsync({type:'blob',compression:'DEFLATE',compressionOptions:{level:6}});saveMode=await saveBlobPreferred(blob,suggestedName)}let verified=saveMode==='directory'||saveMode==='desktop-workspace';if(saveMode==='download-unverified')verified=await confirmFallbackDownloadSaved(suggestedName);if(!verified){$('exportMsg').textContent=`文件已触发浏览器下载，但尚未确认保存完成；本次未写入历史。改价 ${tu} 行；价格删除 ${td} 行；库存修改 ${tiu} 行；库存删除 ${ti} 行。`;$('exportMsg').className='hint warn';showToast('已触发下载；由于未确认文件保存完成，历史记录没有更新。','warn',6000);return}const histCommit=commitSuccessfulExportToHistory(storeAssignments);saveSuccessfulExportSnapshots();state._exportStoreAssignmentsDraft=new Map();const where=saveMode==='desktop-workspace'?'已保存到“已处理表格/店铺/日期”文件夹':(saveMode==='directory'?'已保存到授权文件夹':'已确认浏览器下载完成'),renameNote=desktopMode?`；原表已关联改名 ${sourceRenamed} 个${sourceRenameWarnings.length?`（${sourceRenameWarnings.length} 个改名失败，导出文件不受影响）`:''}`:'';$('exportMsg').textContent=`完成：${state.books.length} 个文件；${where}${renameNote}；改价 ${tu} 行；价格删除 ${td} 行；库存修改 ${tiu} 行；库存删除 ${ti} 行${tn?`；原价为空保留 ${tn} 行`:''}；已写入历史 ${histCommit.saved} 个SPU${histCommit.moved?`（${histCommit.moved} 个店铺归属已更新）`:''}。`;$('exportMsg').className=sourceRenameWarnings.length?'hint warn':'hint ok';showToast(sourceRenameWarnings.length?`导出成功，但有 ${sourceRenameWarnings.length} 个原表改名失败。`:`导出成功，${sourceRenamed} 个原表已按导出名称关联改名。`,sourceRenameWarnings.length?'warn':'ok',6000);refreshDesktopWorkspace(false).catch(()=>{})}
@@ -1328,8 +1329,11 @@
 
 if($('desktopOpenInbox'))$('desktopOpenInbox').onclick=()=>window.temuDesktop.openWorkspaceInbox();
 if($('desktopOpenProcessed'))$('desktopOpenProcessed').onclick=()=>window.temuDesktop.openWorkspaceProcessed();
+if($('desktopOpenLogs'))$('desktopOpenLogs').onclick=()=>window.temuDesktop.openWorkspaceLogs();
 if($('desktopScanInbox'))$('desktopScanInbox').onclick=()=>refreshDesktopWorkspace(true);
-setTimeout(()=>{refreshDesktopWorkspace(true).catch(()=>{});window.temuDesktop?.getSettings?.().then(st=>{if(st?.workspaceAutoScan!==false){clearInterval(desktopWorkspaceTimer);desktopWorkspaceTimer=setInterval(()=>refreshDesktopWorkspace(true).catch(()=>{}),(Math.max(3,Number(st.workspaceScanSeconds)||8))*1000)}})},900);
+function applyWorkspaceAutoScanSettings(st={}){clearInterval(desktopWorkspaceTimer);desktopWorkspaceTimer=null;if(st?.workspaceAutoScan!==false){desktopWorkspaceTimer=setInterval(()=>refreshDesktopWorkspace(true).catch(()=>{}),(Math.max(3,Number(st.workspaceScanSeconds)||8))*1000)}}
+setTimeout(()=>{refreshDesktopWorkspace(true).catch(()=>{});window.temuDesktop?.getSettings?.().then(applyWorkspaceAutoScanSettings)},900);
+window.temuDesktop?.onSettingsChanged?.(st=>{applyWorkspaceAutoScanSettings(st);refreshDesktopWorkspace(false).catch(()=>{})});
